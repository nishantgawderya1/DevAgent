# DevAgent
DevAgent is an autonomous software repair agent that reads a GitHub issue, navigates your codebase using AST-aware retrieval, writes a fix inside a Docker sandbox, runs your existing tests, and opens a pull request — without you touching a single line of code.  Not a copilot. Not autocomplete. An actual agent that ships working code.
