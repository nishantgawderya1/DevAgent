[pytest]
# Only DevAgent's own tests. fixtures/ holds standalone repos that DevAgent is
# pointed at -- they have their own suites, deliberately failing ones, and are
# not importable from here.
testpaths = tests
norecursedirs = fixtures venv .venv .git __pycache__
