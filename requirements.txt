# Retrieval layer (implemented)
# tree-sitter core must be >=0.24 to load the ABI-15 language packs below.
tree-sitter>=0.24
tree-sitter-python>=0.25
tree-sitter-javascript>=0.25

# Embeddings: local sentence-transformers model (all-MiniLM-L6-v2, 384-dim, no API key)
sentence-transformers>=2.2

# Vector store + keyword search
qdrant-client>=1.7
rank-bm25>=0.2.2

# Agent (LangGraph state machine + LLM via OpenRouter)
langgraph>=0.2
openai>=1.0

# Backend: webhook, sandboxed test execution, GitHub integration
fastapi>=0.111
uvicorn[standard]>=0.30
PyGithub>=2.3
docker>=7.0

# Config
python-dotenv>=1.0

# Tests
pytest>=8.0
